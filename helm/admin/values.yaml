replicaCount: 1

image:
  repository: jiwi/objevplzen
  tag: admin
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3001

env:
  nextPublicApiUrl: "http://backend:3000/api"

resources: {}
