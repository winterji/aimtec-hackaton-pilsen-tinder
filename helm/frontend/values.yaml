replicaCount: 1

image:
  repository: jiwi/objevplzen
  tag: frontend-amd
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3002

env:
  nextPublicApiUrl: "http://backend:3000/api"

resources: {}
